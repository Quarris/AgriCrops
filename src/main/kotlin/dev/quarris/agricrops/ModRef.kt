package dev.quarris.agricrops

import org.apache.logging.log4j.LogManager
import org.apache.logging.log4j.Logger

object ModRef {
    const val ID = "agricrops"
    val Logger: Logger = LogManager.getLogger(ID)
}